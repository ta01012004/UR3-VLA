# Python deps for RLBench smoke video on Linux HPC.
# CoppeliaSim 4.1.0 is installed separately by install_hpc.sh.
# Recommended Python: exactly 3.8.x for best PyRep/CoppeliaSim compatibility.

numpy<2
scipy
Pillow
imageio
imageio-ffmpeg
opencv-python-headless
matplotlib
gym
gymnasium
cffi>=1.0.0
pyquaternion
natsort

# PyRep/RLBench are installed by install_hpc.sh with pinned commits after
# COPPELIASIM_ROOT is exported, because PyRep needs that variable at install time.
